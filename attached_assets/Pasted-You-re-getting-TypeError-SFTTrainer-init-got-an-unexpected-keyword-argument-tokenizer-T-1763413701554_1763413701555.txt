You’re getting:

TypeError: SFTTrainer.__init__() got an unexpected keyword argument 'tokenizer'


This happens because you’re using the new TRL version (0.9 / 0.10), where the API changed.

✅ WHAT’S WRONG

Your SFTTrainer(...) call looks something like:

trainer = SFTTrainer(
    model=model,
    tokenizer=tokenizer,
    train_dataset=train_dataset,
    ...
)


But in the new TRL:

❌ You CANNOT pass tokenizer= directly.
✔ You MUST pass a ScriptArguments style config.
✅ HERE IS THE FIX

Open this file:

src/baseline_sft/train_sft.py


Find the area where you create the trainer.

Replace the whole block with THIS updated version (works with TRL 0.9+):

✅ PASTE THIS VERSION (copy/paste exactly)
from trl import SFTTrainer, SFTConfig
from transformers import AutoTokenizer, AutoModelForCausalLM

def main():
    model_name = "deepseek-ai/DeepSeek-R1-Distill-Llama-8B"

    tokenizer = AutoTokenizer.from_pretrained(model_name)
    tokenizer.pad_token = tokenizer.eos_token

    model = AutoModelForCausalLM.from_pretrained(
        model_name,
        device_map="auto",
        torch_dtype="auto",
    )

    # Load your dataset here
    from datasets import load_dataset
    ds = load_dataset("json", data_files="data/gsm8k_train.jsonl")["train"]

    # Format function (ensure input_ids + labels created)
    def formatting_func(example):
        prompt = (
            "You are a careful math tutor. Solve step by step.\n\n"
            f"Problem:\n{example['question']}\n\nSolution:"
        )
        target = example["answer"]
        return prompt, target

    # TRL v0.9+ config
    sft_config = SFTConfig(
        output_dir="checkpoints/sft",
        overwrite_output_dir=True,
        max_seq_length=2048,
        num_train_epochs=1,
        per_device_train_batch_size=1,
        gradient_accumulation_steps=8,
        learning_rate=5e-5,
        logging_steps=10,
        save_strategy="epoch",
    )

    trainer = SFTTrainer(
        model=model,
        tokenizer=tokenizer,
        train_dataset=ds,
        formatting_func=formatting_func,
        args=sft_config,
    )

    trainer.train()
    trainer.save_model("checkpoints/sft")
    tokenizer.save_pretrained("checkpoints/sft")


if __name__ == "__main__":
    main()

🔥 WHY THIS WORKS

TRL removed the tokenizer= and dataset_text_field= arguments.

Now the trainer accepts:

formatting_func → returns (prompt, answer)

args=SFTConfig(...) instead of **kwargs

This new version is 100% compliant with current TRL.